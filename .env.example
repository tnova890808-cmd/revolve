# Revolve environment variables — copy to .env.local and fill in.
# NEVER commit real secrets. Only this example file (with empty placeholders) belongs in git.

# App
VITE_APP_NAME=Revolve
VITE_APP_URL=

# API / backend base URL
VITE_API_BASE_URL=

# Add Revolve-specific keys below. Do NOT reuse any Study Pal Academy values.
